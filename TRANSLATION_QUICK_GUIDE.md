# 🚀 تطبيق الترجمة - دليل سريع

## ✅ تم إنجازه (100%)

### البنية التحتية
- ✅ `contexts/LanguageContext.tsx` - نظام إدارة اللغة
- ✅ `lib/translations.ts` - **ملف شامل بجميع النصوص** 
- ✅ `app/layout.tsx` - LanguageProvider

### المكونات المحدثة
- ✅ `components/navigation.tsx`
- ✅ `components/footer.tsx`
- ✅ `app/page.tsx` (الرئيسية)
- ✅ `app/about/page.tsx`

---

## 📝 الصفحات المتبقية (11 صفحة)

**ملاحظة**: جميع نصوص هذه الصفحات موجودة بالفعل في `lib/translations.ts`!

### 🎯 طريقة التطبيق السريعة

لكل صفحة، اتبع هذه الخطوات:

#### 1. أضف في بداية الملف:
```typescript
"use client" // إذا لم تكن موجودة

import { useLanguage } from "@/contexts/LanguageContext"
```

#### 2. في المكون:
```typescript
export default function PageName() {
  const { t, language } = useLanguage()
  // ... rest of code
}
```

#### 3. استبدل النصوص:
```typescript
// قبل:
<h1>حاسبة المعدل</h1>

// بعد:
<h1>{t('calc.title')}</h1>
```

---

## 📋 قائمة الصفحات مع مفاتيحها

### 1. **Calculator** (`app/calculator/page.tsx`)
```typescript
const { t, language } = useLanguage()

// العناوين
{t('calc.title')} // "حاسبة المعدل التراكمي"
{t('calc.subtitle')} // "احسب معدلك بدقة وسهولة"
{t('calc.add_unit')} // "إضافة وحدة"
{t('calc.calculate')} // "احسب المعدل"
{t('calc.reset')} // "إعادة تعيين"

// الدرجات
{t('calc.pass')} // "نجاح"
{t('calc.merit')} // "جيد"
{t('calc.distinction')} // "امتياز"
```

### 2. **Chat/Community** (`app/chat/page.tsx`)
```typescript
{t('chat.title')} // "مجتمع BTEC"
{t('chat.subtitle')} // "ت

واصل مع زملائك الطلاب"
{t('chat.select_room')} // "اختر غرفة المحادثة"
{t('chat.type_message')} // "اكتب رسالتك..."
{t('chat.send')} // "إرسال"
```

### 3. **Resources** (`app/resources/page.tsx`)
```typescript
{t('resources.title')} // "الموارد التعليمية"
{t('resources.subtitle')} // "جميع المواد التعليمية في مكان واحد"
{t('resources.search')} // "ابحث في الموارد..."
{t('resources.filter')} // "تصفية"
{t('resources.download')} // "تحميل"
```

### 4. **Guide** (`app/guide/page.tsx`)
```typescript
{t('guide.title')} // "دليل الطالب"
{t('guide.subtitle')} // "كل ما تحتاجه للنجاح في دراستك"
{t('guide.getting_started')} // "البدء"
{t('guide.programs')} // "البرامج الدراسية"
{t('guide.grading')} // "نظام الدرجات"
```

### 5. **FAQ** (`app/faq/page.tsx`)
```typescript
{t('faq.title')} // "الأسئلة الشائعة"
{t('faq.subtitle')} // "إجابات على أكثر الأسئلة شيوعاً"
{t('faq.search')} // "ابحث في الأسئلة..."
```

### 6. **Profile** (`app/profile/page.tsx`)
```typescript
{t('profile.title')} // "الملف الشخصي"
{t('profile.edit')} // "تعديل الملف الشخصي"
{t('profile.name')} // "الاسم"
{t('profile.email')} // "البريد الإلكتروني"
{t('profile.save')} // "حفظ التغييرات"
```

### 7. **Dubai Roadmap** (`app/dubai-roadmap/page.tsx`)
```typescript
{t('roadmap.title')} // "خارطة الطريق الدراسية"
{t('roadmap.subtitle')} // "رحلتك التعليمية في BTEC"
{t('roadmap.level')} // "المستوى"
{t('roadmap.units')} // "الوحدات"
```

### 8. **Leaderboard** (`app/leaderboard/page.tsx`)
```typescript
{t('leaderboard.title')} // "لوحة المتصدرين"
{t('leaderboard.subtitle')} // "أفضل الطلاب المتفوقين"
{t('leaderboard.rank')} // "الترتيب"
{t('leaderboard.student')} // "الطالب"
{t('leaderboard.gpa')} // "المعدل"
```

### 9. **Bookmarks** (`app/bookmarks/page.tsx`)
```typescript
{t('nav.bookmarks')} // "المفضلة"
{language === 'ar' ? 'المواد المحفوظة' : 'Saved Items'}
```

### 10. **Messages** (`app/messages/page.tsx`)
```typescript
{t('tool.messages')} // "رسائل"
{language === 'ar' ? 'صندوق الرسائل' : 'Message Inbox'}
```

### 11. **Programs** (`app/programs/page.tsx`)
```typescript
{t('guide.programs')} // "البرامج الدراسية"
{language === 'ar' ? 'جميع البرامج المتاحة' : 'All Available Programs'}
```

---

## 🎨 أنماط شائعة

### للأزرار:
```typescript
<Button>
  {t('common.save')}
  <Icon className={`w-4 h-4 ${language === 'ar' ? 'mr-2' : 'ml-2'}`} />
</Button>
```

### للنصوص المشروطة:
```typescript
{language === 'ar' 
  ? 'نص معقد بالعربية' 
  : 'Complex English text'}
```

### لاتجاه الأيقونات:
```typescript
<ArrowRight className={`${language === 'ar' ? 'rotate-180' : ''}`} />
```

---

## ⚡ نصائح لتسريع العمل

1. **استخدم Find & Replace** في VS Code:
   - اضغط `Ctrl+H`
   - ابحث عن: `"العنوان الثابت"`
   - استبدل بـ: `{t('page.title')}`

2. **استخدم Multi-Cursor** للنصوص المتكررة:
   - `Ctrl+D` لتحديد نفس الكلمة
   - عدل الكل مرة واحدة

3. **ابدأ بالعناوين الرئيسية** ثم انتقل للتفاصيل

4. **اختبر أثناء العمل** بتغيير اللغة من الموقع

---

## ✨ النتيجة النهائية

بعد الانتهاء من تطبيق هذه الخطوات على الـ11 صفحة المتبقية، سيكون لديك:

✅ موقع **كامل** بلغتين (عربي/إنجليزي)
✅ تبديل **فوري** بين اللغات
✅ حفظ اللغة في المتصفح
✅ اتجاه **ص حيح** (RTL/LTR)
✅ تجربة مستخدم **احترافية**

---

## 🎯 التقدير الزمني

- **Calculator**: 15-20 دقيقة (معقدة)
- **باقي الصفحات**: 5-10 دقائق لكل صفحة
- **المجموع**: ~2-3 ساعات عمل فعلي

---

**💡 ملاحظة مهمة**: 
النظام **جاهز بالكامل**! كل النصوص موجودة في `lib/translations.ts`. 
كل ما عليك فعله هو استبدال النصوص الثابتة بـ `t('key')` فقط! 🚀

---

Created by AF BTEC Translation System
المفاتيح موجودة في: `lib/translations.ts`
